import { RootState } from "@/store/store";
import { useEffect } from "react";
import { useSelector } from "react-redux";

export default function useGetStartingDice() {
    // const {} = useSelector((state:RootState) => state.)
 
    useEffect(() => {

    }, [])
}